id: dsq-1038288815
date: 2013-09-11T03:35:10.0000000-07:00
name: anil
avatar: https://disqus.com/api/users/avatars/mandlaanilbabu.jpg
message: '<p>This is my actual code ,Edit multiple images successfully edited,</p><p>1) At the time of Edit single image not edited</p><p>[HttpPost]<br>public ActionResult Edit(int id, Product collection, HttpPostedFileBase[] MultipleFiles)<br>{<br>Product p = db.Products.Single(e =&gt; e.TagID == id);<br>//List&lt;string&gt; blobs = new List&lt;string&gt;();</p><p>if (MultipleFiles != null)<br>{<br>foreach (var fileBase in MultipleFiles)<br>{<br>if (fileBase != null &amp;&amp; fileBase.ContentLength &gt; 0)<br>{<br>// Retrieve a reference to a container <br>CloudBlobContainer blobContainer = _myBlobStorageService.GetCloudBlobContainer();<br>CloudBlob blob = blobContainer.GetBlobReference(fileBase.FileName);</p><p>// Create or overwrite the "myblob" blob with contents from a local file<br>blob.UploadFromStream(fileBase.InputStream);<br>}<br>}<br>//}<br>List&lt;string&gt; blobs = new List&lt;string&gt;();</p><p>foreach (var fileBase in MultipleFiles)<br>{<br>CloudBlobContainer blobContainer1 = _myBlobStorageService.GetCloudBlobContainer();<br>CloudBlob blob1 = blobContainer1.GetBlobReference(fileBase.FileName);<br>//p.Image = blob1.Uri.ToString();<br>blobs.Add(blob1.Uri.ToString());<br>}</p><p>p.Image = blobs.ElementAt(0).ToString();<br>p.Image1 = blobs.ElementAt(1).ToString();<br>} <br>// TODO: Add update logic here<br>//p.Image = collection.Image;<br>p.Name = collection.Name;<br>p.Price = collection.Price;<br>p.Description = collection.Description;<br>//p.Image = collection.Image;<br>// p.Image1 = collection.Image1;<br>db.SubmitChanges();<br>return RedirectToAction("Index");</p><p>}</p>'
